# Keep build context small / avoid copying host cruft into the image.
.git
.github
**/__pycache__
**/*.pyc
# PeTTa is cloned inside the image, not copied from context:
PeTTa
PeTTa-*
# Local build outputs you don't want baked in:
*.log
